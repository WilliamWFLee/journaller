cmd_config() {
  "config_$1" "${@:2}"
}

config_new() {
  if config_file_exists; then
    log_info "~/.journallerrc already exists"
  else
    log_info "Creating ~/.journallerrc"
    touch ~/.journallerrc
  fi
}

config_dir() {
  if [[ -z "$1" ]]; then
    get_dir
  else
    set_dir "$1"
  fi
}
