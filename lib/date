current_date() {
  date +'%Y/%m/%d'
}

current_time() {
  date +'%H:%M'
}

path_to_date() {
  echo -n "$1" | sed 's,/,-,g'
}

date_to_path() {
  echo -n "$1" | sed 's,-,/,g'
}

extract_date_from_path() {
  sed -nE 's,.*([[:digit:]]{4}/[[:digit:]]{2}/[[:digit:]]{2}),\1,p'
}
