#!/bin/bash

. ~/.journalrc

dir="$JOURNAL_DIR/$(date +'%Y/%m/%d')"

mkdir -p "$dir"
echo -ne "**$(date +'%H:%M')**\n\n" >> "$dir/text.md"
vim "$dir/text.md"

if [[ "$@" == *"--encrypt"* ]]; then  
  encrypt-dir $dir
fi

